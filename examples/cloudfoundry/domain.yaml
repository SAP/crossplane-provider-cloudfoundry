---
apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha1
kind: Domain
metadata:
  namespace: default
  name: my-domain
spec:
  forProvider:
    name: dev.orchestrator.io
    orgRef:
      name: my-org
    internal: false