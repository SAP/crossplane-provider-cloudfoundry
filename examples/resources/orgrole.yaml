---
apiVersion: cloudfoundry.crossplane.io/v1alpha1
kind: OrgRole
metadata:
  namespace: default
  name: my-org-auditor-daniel
spec:
  forProvider:
    type: "Manager"
    orgRef:
      name: my-org
    username: "user1@example.com"
    origin: sap.ids
