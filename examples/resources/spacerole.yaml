---
apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha2
kind: SpaceRole
metadata:
  namespace: default
  name: my-space-auditor
spec:
  forProvider:
    type: Auditor
    spaceRef:
      name: my-space
    username: 3@example.com
    origin: sap.ids

---
apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha2
kind: SpaceRole
metadata:
  namespace: default
  name: my-space-developer-daniel
spec:
  forProvider:
    type: Developer
    spaceRef:
      name: my-space
    username: 1@example.com
    origin: sap.ids

---
apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha2
kind: SpaceRole
metadata:
  namespace: default
  name: my-space-developer
spec:
  forProvider:
    type: Developer
    spaceRef:
      name: my-space
    username: 123@example.com
    origin: sap.ids
