---
apiVersion: cloudfoundry.crossplane.io/v1alpha1
kind: Org
metadata:
  namespace: default
  name: my-org
spec:
  managementPolicies: ["Observe"]
  forProvider: 
    name: cf-dev
  providerConfigRef:
    name: default
