name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '15 10 * * 4'

permissions:
  security-events: write
  packages: read
  actions: read
  contents: read

jobs:
  run-codeql:
    uses: SAP/crossplane-provider-btp/.github/workflows/check-codeql.lib.yaml@dd91b564627eacdf079df3905b5a6efa4a4e9a53
