---
apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha2
kind: SpaceQuota
metadata:
  name: my-space-quota
spec:
  forProvider:
    name: my-space-quota
    orgRef:
      name: my-org
    allowPaidServicePlans: false
    spacesRefs:
      - name: my-space
