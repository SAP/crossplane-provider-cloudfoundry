apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha2
kind: OrgRole
metadata:
  namespace: default
  name: my-org-role
spec:
  forProvider:
    type: "Auditor"
    orgRef:
      name: my-org
    username: "co-test-user@groups.sap.com"
