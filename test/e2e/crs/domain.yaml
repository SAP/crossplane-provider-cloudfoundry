---
apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha2
kind: Domain
metadata:
  namespace: default
  name: my-domain
spec:
  forProvider:
    name: dev.orchestrator.io
    orgRef:
      name: my-org
    internal: false