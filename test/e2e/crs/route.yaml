---
apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha2
kind: Route
metadata:
  name: e2e-route
spec:
  forProvider:
    domainRef: 
      name: e2e-domain
      policy:
        resolve: Always
    host: hello-co
    spaceRef:
      name: e2e-space
      policy:
        resolve: Always