apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha1
kind: SpaceMembers
metadata:
  namespace: default
  name: my-space-developers
spec:
  forProvider:
    roleType: Developers
    members:
    - username: co-test-user@groups.sap.com
    - username: lasse.friedrich@sap.com
    spaceRef:
      name: my-space
      policy:
        resolve: Always
    enforcementPolicy: Lax
