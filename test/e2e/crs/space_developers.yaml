apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha1
kind: SpaceMembers
metadata:
  namespace: default
  name: e2e-space-developers
spec:
  forProvider:
    roleType: Developers
    members:
    - username: co-test-user@example.com
    spaceRef:
      name: e2e-space
      policy:
        resolve: Always
    enforcementPolicy: Lax
