apiVersion: cloudfoundry.btp.orchestrate.cloud.sap/v1alpha2
kind: SpaceRole
metadata:
  namespace: default
  name: e2e-space-role
spec:
  forProvider:
    type: Developer
    spaceRef:
      name: e2e-space
      policy:
        resolve: Always
    username: "123@example.com"
