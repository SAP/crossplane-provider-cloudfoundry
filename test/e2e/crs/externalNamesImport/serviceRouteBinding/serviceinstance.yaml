apiVersion: cloudfoundry.crossplane.io/v1alpha1
kind: ServiceInstance
metadata:
  name: upgrade-test-serviceinstance
spec:
  providerConfigRef:
    name: default
  forProvider:
    type: user-provided
    name: upgrade-test-serviceroutebinding-serviceinstance
    spaceRef:
      name: upgrade-test-import-space
      policy:
        resolve: Always
    routeServiceUrl: https://example-route-service.local/forward
