apiVersion: cloudfoundry.crossplane.io/v1alpha1
kind: ServiceInstance
metadata:
  name: import-test-scb-serviceinstance
spec:
  providerConfigRef:
    name: default
  forProvider:
    type: user-provided
    name: import-test-scb-serviceinstance
    spaceRef:
      name: upgrade-test-import-space
      policy:
        resolve: Always
