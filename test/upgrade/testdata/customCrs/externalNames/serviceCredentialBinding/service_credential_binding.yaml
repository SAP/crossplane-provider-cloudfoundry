apiVersion: cloudfoundry.crossplane.io/v1alpha1
kind: ServiceCredentialBinding
metadata:
  name: upgrade-test-external-name-service-credential-binding
spec:
  forProvider:
    name: upgrade-test-external-name-service-credential-binding
    type: key
    serviceInstanceRef:
      name: upgrade-test-scb-serviceinstance
      policy:
        resolve: Always
  providerConfigRef:
    name: default
